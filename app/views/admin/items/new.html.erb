<!DOCTYPE html>
<html lang="ja">
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <body>
      <main>
          <h1>商品新規登録</h1>
          <%= form_with model: @item do |f| %>
            <h4>商品画像</h4>
              <%= f.file_field :image, accept: "image/*" %>
            <h4>商品名</h4>
              <%= f.text_field :name %>
            <h4>商品説明</h4>
              <%= f.text_area :introduction %>
            <h4>ジャンル</h4>
              <%= select(:item, :role, [["ケーキ"], ["焼き菓子"], ["プリン"]]) %>
            <h4>販売価格</h4>
              <%= f.text_field :price %>
            <h4>販売ステータス</h4>
              <%= form.check_box :active %><p>販売中</p><%= form.check_box :active %><p>販売停止中</p>
            <%= f.submit '新規登録' %>
          <% end %>
      </main>
  </body>