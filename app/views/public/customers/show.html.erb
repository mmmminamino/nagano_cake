<!DOCTYPE html>
<html lang="ja">
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
    <main>
        <body>
              <div class="pull-right" style="margin-bottome: 10px;">
                <%= search_field(@search, url:public_items_path) do |f| %>
                  <%= f.label :name, "キーワード" %>
                  <%= f.search_field :name_cont %>
                  <%= f.submit %>
                <% end %>
              </div>
            </header>
            <div class="row">
              <div class="col-xs-12">
                <h3>マイページ</h3>
                <p>
                    <h3>登録情報</h3>
                    <%= link_to "編集する", public_customers_edit_path, class: 'btn btn-default' %>
                </p>
                <table border="1">
                    <tr>
                        <td class="col-xs-2">氏名</td>
                        <td><%= current_customer.last_name %> <%= current_customer.first_name %></td>
                    </tr>
                    <tr>
                        <td>カナ</td>
                        <td><%= current_customer.last_name_kana %> <%=current_customer.first_name_kana %></td>
                    </tr>
                    <tr>
                        <td>郵便番号</td>
                        <td><%= current_customer.postal_code %></td>
                    </tr>
                    <tr>
                        <td>住所</td>
                        <td><%= current_customer.address %></td>
                    </tr>
                    <tr>
                        <td>電話番号</td>
                        <td><%= current_customer.telephone_number %></td>
                    </tr>
                    <tr>
                        <td>メールアドレス</td>
                        <td><%= current_customer.email %></td>
                    </tr>
                </table>
                <h3>配送先</h3><%= link_to "一覧を見る",  class: 'btn btn-default' %>
                <h3>注文履歴</h3><%= link_to "一覧を見る", public_orders_path,  class: 'btn btn-default' %>
              </div>
            </div>
        </body>
    </main>
</html>